service cloud.firestore {
  match /databases/{database}/documents {
    match /stores/{userId} {
      allow read, write: if request.auth.uid != null;
    }
    match /stores/{userId}/location/{locationId} {
      allow read, write: if request.auth.uid != null;
    }
  }
}
